# esale-web

Frontend application for the eSale platform, built with Next.js, TypeScript, Tailwind CSS, React Query, and shadcn.

## Getting Started

```bash
npm install
npm run dev
```

Open `http://localhost:3000` in the browser.
